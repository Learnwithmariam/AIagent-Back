{
  "name": "gk-btu-students-api",
  "version": "2.0.0",
  "private": true,
  "description": "G.K. BTU Students API — AI teaching agent (OpenRouter), proctored exams and daily digest on Cloudflare Workers",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "build": "wrangler deploy --dry-run --outdir dist",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@noble/hashes": "^2.4.0",
    "fflate": "^0.8.3",
    "hono": "^4.13.8",
    "unpdf": "^1.8.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260923.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.137.0"
  }
}
